<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorControlHighlight">@android:color/darker_gray</item>
    </style>

    <!--<style name="BottomSheet.TopDivider">
        <item name="android:background">@android:color/holo_red_dark</item>
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>-->
    
    <style name="ButtonDialNumTheme" parent="@style/Widget.AppCompat.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textStyle">normal</item>
        <item name="android:fontFamily">SegoeUI Light</item>
        <item name="android:background">?selectableItemBackgroundBorderless</item>
        <!--<item name="android:background">@android:color/white</item>-->
        <item name="android:textSize">30sp</item>
    </style>

</resources>
